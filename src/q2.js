function myFunction(num1, num2) {
    num1+num2
  }
  
  const result = myFunction(5,5)

  /*
  What is the value of result after calling this function? Why?
  Similar to q1, but instead, it's not returning anything. Therefore the result variable will be undefined. 

  As expected, when I typed the console log command, it returned undefined. 
  */